

<div class="modal fade" id="showCancelToNewModal" tabindex="-1"
     aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-light p-3">
                <h5 class="modal-title" id="exampleModalLabel">@sharedResource["Return Request As New Request"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"
                        aria-label="Close" id="close-modal"></button>
            </div>

            <form asp-area="Admin" asp-page="/ManageRequests/CanceledRequests" asp-page-handler="ReturnRequestToNew" enctype="multipart/form-data">
                <div class="modal-body">
                    <input type="hidden" id="DRequestId" name="RequestId" />

                    <div class="row g-3">

                        <div class="col-lg-12">
                            <div>
                                <label  class="form-label">@sharedResource["Notes"]</label>
                                <textarea name="RequestNotes" class="form-control" placeholder="@sharedResource["Enter Notes"]"></textarea>
                                <span  class="text-danger"></span>
                            </div>
                        </div>


                    </div>
                    <!--end row-->
                </div>
                <div class="modal-footer">
                    <div class="hstack gap-2 justify-content-end">
                        <button type="button" class="btn btn-light"
                                data-bs-dismiss="modal">
                            @sharedResource["Close"]
                        </button>
                        <button type="submit" class="btn btn-success"
                                id="add-btn">
                            @sharedResource["Return Request As New Request"]
                        </button>

                    </div>
                </div>
            </form>
        </div>
    </div>
</div>